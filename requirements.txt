# 运行时依赖（runtime dependencies）：
# 运行本 Skill 的筛选 CLI、PandaData 点时提供者、规则引擎与 JSON/Parquet 物化所需。
# 安装：python -m pip install -r requirements.txt
panda_data==0.0.12
pandas>=2.0,<4
numpy>=1.24,<3
pyarrow>=14,<26
